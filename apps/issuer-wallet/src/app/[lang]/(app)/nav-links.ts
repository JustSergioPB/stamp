import { NavLink } from "@components/stamp/sidebar";
import { Braces } from "lucide-react";

export const NAV_LINKS: NavLink[] = [
  { title: "schemas", icon: Braces, variant: "ghost", href: "/schemas" },
];
